name: Codex Review Follow-up

on:
  issue_comment:
    types: [created]
  pull_request_review:
    types: [submitted]
  pull_request_review_comment:
    types: [created]

permissions:
  contents: read
  issues: write
  pull-requests: write

concurrency:
  group: codex-review-followup-${{ github.event.pull_request.number || github.event.issue.number || github.run_id }}
  cancel-in-progress: false

jobs:
  codex-review-followup:
    if: github.event.sender.login == 'chatgpt-codex-connector[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Codex 피드백 후속 이슈 생성
        uses: actions/github-script@v7
        with:
          script: |
            const owner = context.repo.owner;
            const repo = context.repo.repo;
            const eventName = context.eventName;
            const payload = context.payload;

            function getPullRequestRef() {
              if (eventName === 'issue_comment') {
                if (!payload.issue.pull_request) return null;
                return { number: payload.issue.number };
              }

              if (eventName === 'pull_request_review') {
                return { number: payload.pull_request.number };
              }

              if (eventName === 'pull_request_review_comment') {
                return { number: payload.pull_request.number };
              }

              return null;
            }

            function getFeedback() {
              if (eventName === 'issue_comment') {
                return {
                  id: payload.comment.id,
                  body: payload.comment.body || '',
                  url: payload.comment.html_url,
                  source: 'issue_comment',
                };
              }

              if (eventName === 'pull_request_review') {
                return {
                  id: payload.review.id,
                  body: payload.review.body || '',
                  url: payload.review.html_url,
                  source: 'pull_request_review',
                };
              }

              return {
                id: payload.comment.id,
                body: payload.comment.body || '',
                url: payload.comment.html_url,
                source: 'pull_request_review_comment',
              };
            }

            async function ensureLabel(name, color, description) {
              try {
                await github.rest.issues.getLabel({ owner, repo, name });
              } catch (error) {
                if (error.status !== 404) throw error;
                await github.rest.issues.createLabel({
                  owner,
                  repo,
                  name,
                  color,
                  description,
                });
              }
            }

            async function addLabel(issue_number, name) {
              await github.rest.issues.addLabels({
                owner,
                repo,
                issue_number,
                labels: [name],
              });
            }

            const prRef = getPullRequestRef();
            if (!prRef) return;

            const feedback = getFeedback();
            const body = feedback.body.trim();
            if (!body) return;

            const { data: pr } = await github.rest.pulls.get({
              owner,
              repo,
              pull_number: prRef.number,
            });

            await ensureLabel('codex-feedback', 'D93F0B', '처리할 GitHub Codex 리뷰 피드백이 있습니다.');
            await ensureLabel('codex-reviewed', '0E8A16', 'GitHub Codex가 주요 문제를 찾지 않았습니다.');
            await ensureLabel('codex-follow-up', '5319E7', 'GitHub Codex 리뷰 피드백에서 생성된 후속 이슈입니다.');

            const noMajorIssue = /did(?:n't| not) find any major issues|no major issues|major issues \uC5C6\uC74C/i.test(body);
            if (noMajorIssue) {
              await addLabel(prRef.number, 'codex-reviewed');
              return;
            }

            const genericReviewEnvelope = /Here are some automated review suggestions/i.test(body)
              && eventName === 'pull_request_review';
            if (genericReviewEnvelope) return;

            await addLabel(prRef.number, 'codex-feedback');

            const marker = `codex-review-followup-pr-${prRef.number}`;
            const searchQuery = `repo:${owner}/${repo} is:issue is:open "${marker}"`;
            const { data: search } = await github.rest.search.issuesAndPullRequests({
              q: searchQuery,
              per_page: 1,
            });

            const excerpt = body.length > 3500 ? `${body.slice(0, 3500)}\n\n...` : body;
            const issueBody = [
              `<!-- ${marker} -->`,
              `# Codex 리뷰 후속 처리: PR #${prRef.number}`,
              '',
              '## 원본 PR',
              `- PR: #${prRef.number}`,
              `- Head SHA: \`${pr.head.sha}\``,
              `- 리뷰 출처: ${feedback.source}`,
              `- 리뷰 링크: ${feedback.url}`,
              '',
              '## 리뷰 피드백',
              '',
              excerpt,
              '',
              '## 처리 체크리스트',
              '',
              '- [ ] 피드백이 현재 코드/문서에도 유효한지 확인',
              '- [ ] 수정 또는 보류 여부 판단',
              '- [ ] 필요한 테스트와 검증 실행',
              '- [ ] PR thread 또는 코멘트에 판단 근거를 한국어로 기록',
              '- [ ] merge 전 관련 PR conversation 또는 review thread resolved 처리',
              '- [ ] 실질 변경이 있으면 `@codex review` 재요청',
            ].join('\n');

            if (search.items.length > 0) {
              await github.rest.issues.createComment({
                owner,
                repo,
                issue_number: search.items[0].number,
                body: [
                  `PR #${prRef.number}에 Codex 피드백이 추가되었습니다.`,
                  '',
                  `- Head SHA: \`${pr.head.sha}\``,
                  `- 리뷰 링크: ${feedback.url}`,
                  '',
                  excerpt,
                ].join('\n'),
              });
              return;
            }

            await github.rest.issues.create({
              owner,
              repo,
              title: `Codex 리뷰 후속 처리: PR #${prRef.number}`,
              body: issueBody,
              labels: ['codex-follow-up', 'codex-feedback'],
            });
